"use client";
import { CalendarOff, ShieldCheck } from "lucide-react";
import { useGetMyStaffProfileQuery } from "@/src/redux/features/team/teamApi";
import { useGetPublicSchedulingConfigQuery } from "@/src/redux/features/scheduling/schedulingApi";
import { LoadingState } from "@/src/components/ui/feedback";
const days=["Sun","Mon","Tue","Wed","Thu","Fri","Sat"];
export default function StaffProfilePage(){const {data,isLoading}=useGetMyStaffProfileQuery();const {data:config}=useGetPublicSchedulingConfigQuery();const timezone=config?.timezone||"America/New_York";const time=(value:string)=>new Intl.DateTimeFormat("en-US",{timeZone:timezone,month:"short",day:"numeric",year:"numeric",hour:"numeric",minute:"2-digit"}).format(new Date(value));if(isLoading||!data)return <LoadingState label="Loading profile…"/>;return <div className="space-y-5"><section className="surface p-5"><span className="editorial-kicker">My team profile</span><h1 className="mt-3 text-2xl font-extrabold text-brand-dark">{data.name}</h1><p className="mt-1 text-sm text-muted-foreground">{data.jobTitle||"Cleaner"} · {data.employeeCode||data.email}</p><div className="mt-4 flex flex-wrap gap-2">{data.skills.map(skill=><span key={skill} className="rounded-lg bg-brand-cream px-3 py-1.5 text-xs font-bold text-brand-dark">{skill}</span>)}</div></section><section className="surface p-5"><div className="flex items-center gap-2"><ShieldCheck className="h-4 w-4 text-brand-green"/><h2 className="font-extrabold text-brand-dark">Regular availability</h2></div><div className="mt-4 grid gap-2 sm:grid-cols-2">{data.weeklyHours.map(day=><div key={day.dayOfWeek} className="flex items-center justify-between rounded-xl border border-border p-3"><span className="text-xs font-extrabold text-brand-dark">{days[day.dayOfWeek]}</span><span className="text-xs text-muted-foreground">{day.isAvailable?`${day.start}–${day.end}`:"Unavailable"}</span></div>)}</div></section><section className="surface p-5"><div className="flex items-center gap-2"><CalendarOff className="h-4 w-4 text-brand-green"/><h2 className="font-extrabold text-brand-dark">Time off</h2></div><div className="mt-4 space-y-2">{data.timeOff.map((entry,index)=><div key={index} className="rounded-xl border border-border p-3 text-xs text-muted-foreground">{time(entry.startAt)} → {time(entry.endAt)} {entry.reason?`· ${entry.reason}`:""}</div>)}{!data.timeOff.length?<p className="text-sm text-muted-foreground">No upcoming time-off blocks.</p>:null}</div></section></div>}
