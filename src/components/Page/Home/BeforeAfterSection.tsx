import Link from "next/link";
import { BeforeAfterShowcase } from "@/src/components/Public/BeforeAfterShowcase";
import type { WebsiteHomepageSection } from "@/src/redux/features/website/types";
export default function BeforeAfterSection({section}:{section?:WebsiteHomepageSection}) { return <section className="bg-brand-cream py-24"><div className="container-page grid gap-8 lg:grid-cols-[.8fr_1.2fr] lg:items-center"><div data-cinema-reveal><span className="editorial-kicker">{section?.eyebrow || "Before + after"}</span><h2 className="mt-4 text-4xl font-extrabold tracking-[-.045em] text-brand-dark md:text-5xl">{section?.title || "The difference is in the finishing pass."}</h2><p className="mt-4 max-w-lg leading-7 text-muted-foreground">{section?.subtitle || "Drag across the image to compare the visual reset. Our actual field workflow also supports before/after evidence on assigned jobs."}</p><Link href="/before-after" className="btn-secondary mt-6">See the cleaning standard</Link></div><BeforeAfterShowcase compact /></div></section>; }
